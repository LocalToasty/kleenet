#include "fail.h"

#include <assert.h>

void kleenetstub::expressLinkFailure() {
  assert(0 && "Calling KleeNet methods in KleeNetStub is not supported. You should not link against this lib if you want Net Support.");
}
